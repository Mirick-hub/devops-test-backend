# Приклад локальних environment variables. Скопіюйте цей файл у .env і за потреби змініть
# значення. Сам файл .env НЕ повинен потрапляти в Git (див. .gitignore) — саме так,
# у реальних проєктах, розділяють код і конфігурацію/секрети.

DATABASE_URL=sqlite:///./app.db
APP_ENV=local
